{
  "name": "villanova-3d-recreation",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "data": "node scripts/fetch-campus.mjs",
    "data:refresh": "node scripts/fetch-campus.mjs --refresh",
    "verify": "node scripts/verify-data.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "vite": "^6.0.0"
  }
}
